import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: {
            light: "#e8eaf6",
            main: "#4527a0",
            dark: "#002884",
            contrastText: "#fff",
        },
        secondary: {
            light: "#e3f2fd",
            main: "#1565c0",
            dark: "#ba000d",
            contrastText: "#000",
        },
    },
});
